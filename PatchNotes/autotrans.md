# 🌐 크랙 초월번역기

## 📌 업데이트 내역

### V4.1.7

- 말풍선의 메시지 ID로 `GET /messages/{id}` 단건 조회를 먼저 시도하고, 단건 조회를 지원하지 않거나 `404`가 반환될 때만 `nextCursor` 페이지 검색으로 전환합니다.
- 페이지 검색은 한 번에 50개씩 최대 32페이지까지 진행하며, 같은 cursor가 반복되면 즉시 중단해 오래된 답변 탐색 중 무한 요청이 생기지 않도록 했습니다.
- 리롤로 한 말풍선에 여러 message ID가 남아 있어도 현재 화면에 보이는 답변과 가장 잘 맞는 ID를 선택합니다.
- 이미 번역문으로 교체한 말풍선을 다시 눌렀을 때 서버의 번역문이 아니라 최초 영문 원문으로 재번역합니다. 화면 재마운트로 임시 표시 정보가 사라진 경우에도 최초 원문을 별도로 보존합니다.
- 실제 교체 직전에는 정확한 primary message ID만 다시 확인하고, 유사도 검색으로 다른 답변을 잘못 교체하지 않도록 했습니다.
- 답변 생성 직후에는 같은 ID와 서버 내용이 두 번 연속 안정된 것을 확인한 뒤 번역하며, 기존 진단 출력과 모바일 Edge 보호 로직을 유지합니다.

---

### V4.1.6

- 모바일 Edge 등에서 UTF-8 한글이 깨져 저장된 번역 지침을 불러올 때 자동 복구하고, 복구된 값을 다시 저장하도록 했습니다.
- 커스텀 지침 슬롯의 제목·본문, 치환 슬롯, OOC 저장문에도 같은 복구를 적용했습니다.
- 이중으로 깨진 한글과 일부 바이트만 손상된 문자열도 정상 시퀀스 단위로 복구하되, `café` 같은 정상 라틴 문자는 변경하지 않습니다.
- `.wrtn-markdown` 구조를 찾지 못하는 모바일 화면에서는 말풍선의 실제 표시 텍스트를 대신 읽도록 보완했습니다.
- `assistant`뿐 아니라 `bot`, `character`, `ai` 형태로 표시되는 답변도 AI 답변으로 인식하고, 교체 단계에서도 같은 역할 판정을 사용합니다.
- 모바일 브라우저의 자동 번역이 말풍선 원문을 바꾸지 않도록 `translate="no"`를 적용하고, 답변 탐색 실패 시 메시지 수·말풍선 ID·화면 텍스트·후보 점수를 진단 정보로 표시합니다.

---

### V4.1.5

- 답변이 스트리밍되는 동안 글자 하나가 바뀔 때마다 문서 전체의 번역 버튼과 사이드바를 다시 찾던 작업을 제거했습니다.
- 새로 추가되거나 다시 마운트된 영역만 확인해 필요한 번역 버튼을 붙이고, 번역기 자체가 만든 화면 변경은 감시 대상에서 제외합니다.
- 현재 화면에 임시 적용 중인 번역문이 있을 때만 말풍선 동기화를 실행해 평상시 감시 비용을 줄였습니다.
- 번역 버튼 또는 사이드바 설정 버튼만 사라진 경우 해당 부분만 복구하고, 방 이동 시에는 필요한 전체 동기화를 유지합니다.
- 테마 감지, OOC 자동 주입, 캐시된 번역 결과, 즉시 번역 및 새로고침 없는 말풍선 교체 기능은 그대로 유지됩니다.

---

### V4.1.4
- 크랙이 답변 ID를 내부 객체에 넣거나 같은 본문을 화면에 중복 렌더링하는 경우 정상 답변도 찾지 못하던 문제를 수정했습니다. 조회 범위와 응답 형식 호환성도 함께 확대했으며, DeepSeek V4 Flash·Pro에서도 번역 요청 전에 멈추던 현상을 보완했습니다.

---

### V4.1.3

- `Gemini 3.8 Flash`를 설정창과 결과창의 모델 목록에 추가하고, 추론 레벨 및 사용 금액 계산을 지원합니다.
- 번역본으로 메시지를 교체한 뒤 새로고침하지 않아도 현재 말풍선에 즉시 반영되도록 변경했습니다. 마크다운 서식은 유지하고 위험한 HTML은 제거합니다.
- 번역할 답변을 찾는 방식을 개선했습니다. 이전 답변, 스트리밍 중인 답변, 링크·코드 블록이 포함된 답변, 모바일·PC용 요소가 겹친 경우에도 사용자가 누른 답변을 우선 확인하며 다른 메시지가 잘못 교체되지 않도록 했습니다.
- `번역 방식`에 커스텀 슬롯 기능을 추가했습니다. 슬롯별 제목과 전용 번역 지침을 만들 수 있으며, 입력한 내용과 마지막 선택 슬롯은 현재 브라우저에 자동 저장됩니다.
- 커스텀 슬롯 추가·삭제·삭제 취소를 지원하고, 설정창과 번역 결과창의 드롭다운에서 저장한 슬롯을 빠르게 전환할 수 있습니다.
- 동일한 답변을 연속으로 교체하는 동작을 차단했습니다. 서버 저장에는 성공했지만 현재 화면에 표시하지 못한 경우에는 API를 다시 호출하지 않고 `화면 표시 다시 시도`로 복구할 수 있습니다.

- 알려진 주의사항

- 교체 직후의 말풍선은 임시 미리보기이므로 코드 블록이 깨져 보일 수 있습니다. 저장된 번역문 자체가 깨진 것은 아니며, 페이지를 새로고침하면 정상 표시됩니다.
- 교체 후 새로고침하기 전에 크랙의 `수정`을 누르면 편집창에 번역 전 영문이나 이전 내용이 나타날 수 있습니다. 이 상태로 저장하면 번역이 이전 내용으로 롤백될 수 있으므로, **번역문 교체 → 페이지 새로고침 → 수정** 순서로 사용해 주세요.


---

## V4.1.2

- Gemini 3.7 Flash를 설정 및 리롤 모델 목록에 추가하고 예상 비용·추론 레벨을 지원합니다.
- 번역 전용 시스템 규칙을 추가하여 원문의 요청에 답하거나 롤플레잉을 이어가는 현상을 방지했습니다.
- 결과창 우측 상단에 설정창과 동일한 닫기 버튼을 추가했습니다.
- 모바일에서 닫기·치환·결과 선택 버튼이 잘리지 않도록 스크롤, 안전 영역 및 터치 크기를 개선했습니다.
- 리롤 결과와 직접 수정한 내용을 보존하고, 결과 목록에서 원하는 번역본을 선택해 적용할 수 있습니다.
- 새로고침 전까지 메시지별 번역 결과를 보존하며, 같은 번역 아이콘을 누르면 API 재호출 없이 결과창을 다시 엽니다.
- 연속 클릭으로 번역 요청이 중복되거나 이전 응답이 다른 결과에 섞이는 문제를 방지했습니다.

---

## V4.1.1

* PC 환경에서 설정창과 번역 결과창의 가로폭을 넓혀 가독성을 개선했습니다.
* 기존 🅰️ 이모지 아이콘을 전용 SVG 번역 아이콘으로 변경했습니다.
* 사이드바의 아이콘 크기, 글자 크기, 간격과 정렬을 크랙 기본 메뉴와 통일했습니다.
* 채팅 스트리밍 및 다른 확장프로그램과 함께 사용할 때 불필요한 DOM 탐색과 반복 작업을 줄여 성능을 최적화했습니다.
* 테마 감지 및 OOC 설정 조회 방식을 최적화했습니다.

---

## V4.0

* DeepSeek API 및 DeepSeek 모델을 추가했습니다.
* 사용자 입력 하단에 지정한 OOC 문구를 자동 삽입하는 기능을 추가했습니다.
* 지정한 턴 수가 지나면 이전에 삽입된 OOC가 자동으로 삭제됩니다.
* 타 확장프로그램과 구분하기 쉽도록 번역 아이콘을 🅰️로 변경했습니다.

---

## V3.4

* 이미지 링크 보호 로직을 추가했습니다.
* 번역 중 원문의 이미지 `![]()`가 AI의 재구성으로 삭제되거나 깨지는 현상을 크게 개선했습니다.

### ⚠️ 알려진 문제

* AI가 보호 태그까지 변형할 경우 이미지가 누락되거나 `===IMG_0===` 같은 문자열이 노출될 수 있습니다.
* 해당 오류 발생 시 결과창의 `↻ 다시 돌리기` 버튼으로 재번역해주세요.
* 일반 텍스트 링크 `[text](url)`는 번역 과정에서 간헐적으로 소실될 수 있습니다.

---

## V3.3

* `Gemini 3.5 Flash`를 설정 및 리롤 모델 목록에 추가했습니다.
* 프롬프트 프리셋 변경이 정상적으로 적용되지 않던 오류를 수정했습니다.
* 한글 전용·영문 혼용 번역 지침의 형식 보정 규칙을 강화했습니다.
* 설정 팝업 높이를 줄이고 모바일 레이아웃을 더욱 압축했습니다.
* 하단의 `최신 답변 바로 번역 (팝업 없이)` 버튼을 제거했습니다.

---

## V3.2

* 번역 후 메시지의 특수문자가 사라지는 오류를 수정했습니다.

---

## V3.1

* 번역 결과의 키워드를 한 번에 교체할 수 있는 기능을 추가했습니다.

<img width="680" height="617" alt="image" src="https://github.com/user-attachments/assets/b9cda7f0-d2f6-424b-a474-af5b9671cddd" />

---

## V3.0

* Firebase API가 Gemini 3.1 모델과 호환되도록 개선했습니다.
* 번역 1회당 예상 금액과 입력·출력·추론 토큰 사용량을 표시합니다.

<img width="812" height="673" alt="image" src="https://github.com/user-attachments/assets/b41bff2f-de04-4f5b-b5ba-271c9ecb4136" />

* 모델별 추론 레벨 조절 기능을 추가했습니다.

<img width="367" height="209" alt="image" src="https://github.com/user-attachments/assets/2b09d4f7-8cde-47e6-bbf4-dc5d4ed6fa36" />

* 전체 UI를 개편했습니다.
* 설정창을 크랙 사이드바 메뉴에 통합했습니다.

<img width="251" height="443" alt="image" src="https://github.com/user-attachments/assets/e11ca40f-e47e-40ac-8c3c-58ffd6818a6d" />

* 각 메시지에서 바로 번역할 수 있도록 메시지 버튼을 추가했습니다.

---

## V2.8

* 번역 결과창에서 결과를 직접 수정할 수 있도록 변경했습니다.
* Gemini 2.5 Pro 모델을 추가했습니다.
* Firebase Vertex AI API를 지원합니다.
* 번역 실행 전 설정값이 자동 저장되도록 개선했습니다.

> **참고:** 당시 Firebase Vertex AI는 Gemini 2.5 Pro만 지원했습니다.  
> 드래그 부분 번역 기능은 호환성 문제로 2025-06-18 삭제되었습니다.

---

## V2.6

* 모바일과 PC 화면을 전환할 때 설정 버튼이 화면 밖으로 사라지던 문제를 수정했습니다.

---

## V2.5

* 설정 버튼을 화면 어디로든 드래그해 이동할 수 있도록 변경했습니다.
* `✨ 번역하기` 버튼 크기를 기존의 작은 크기로 되돌렸습니다.

---

## V2.4

* 모바일 환경 호환성을 개선했습니다.
* 안내창과 번역 버튼이 화면 중앙에 표시되도록 수정했습니다.

---

# 이전 버전

## V2.3 — 첫 릴리스

* 첫 릴리스.
* PC 환경을 기준으로 제작되었습니다.
